# 02-reference

Durable source material Cowork reads for context: specs, standing notes, a knowledge base, style guides, prior work worth reusing.

Distinct from `00-inbox/` (transient) and the places you produce work (`01-projects/`, `content/`, `reviews/`). Reference is what you keep at the ready and point Cowork at. Add to it deliberately, not by default.
